import React from 'react'

interface INav{
    title: string;
    url: string;
}

export default function Nav({title, url}: INav) {
    return (
    <li className="dropdown first">
        
    </li>
    )
}
